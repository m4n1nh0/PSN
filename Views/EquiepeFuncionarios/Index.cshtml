@model IEnumerable<PSN2018.Models.EquiepeFuncionarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.intEquipeID_FK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.intFuncID_FK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.strTipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.intStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.intEquipeID_FK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.intFuncID_FK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.strTipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.intStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
