@model IEnumerable<PSN2018.Models.SkillFuncionarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.intFuncID_FK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.intSkillID_FK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.decPerc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.intFuncID_FK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.intSkillID_FK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.decPerc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
